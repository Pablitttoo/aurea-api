spring.application.name=AureaAPI
server.port=8080

# --- CONEXIÓN SUPABASE (PostgreSQL) ---
# 1. URL JDBC construida con tu Host y Puerto (5432)
# Nota: Agregamos '?sslmode=require' porque Supabase exige conexión segura.
spring.datasource.url=jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:5432/postgres?sslmode=require

# 2. Usuario
spring.datasource.username=postgres.ebkhpkftycqzircqzsbi

# 3. Contraseña
spring.datasource.password=J6ze71M3yLK52x3n

# 4. El conductor
spring.datasource.driver-class-name=org.postgresql.Driver

# --- CONFIGURACIÓN JPA (Hibernate) ---
# Esto hará que Spring Boot cree las tablas 'products' automáticamente en Supabase
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- SWAGGER CONFIGURATION ---
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs